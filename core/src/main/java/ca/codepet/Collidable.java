package ca.codepet;

/**
 * The Collidable interface for objects that can collide with each other.
 * Contains the base functionality for all collidable objects.
 */
public interface Collidable {
    float getX();

    float getWidth();

    int getRow();
}